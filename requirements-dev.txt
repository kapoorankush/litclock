ruff>=0.16.0
pytest>=9.1.1
pytest-mock>=3.15.1
pyyaml>=6.0.3
# freetype-py moved to requirements.in at Stage 2 (dev#531) — it is now a
# runtime dependency of src/quote_renderer.py, pinned in the main lock.
